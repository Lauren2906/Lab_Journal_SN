---
title: "Lab journal social networks"
author: "Lauren Dirix"
bibliography: references.bib
---

```{r, install remotely, globalsettings, echo=FALSE, warning=FALSE, results='hide'}
#install.packages("remotes")
#remotes::install_github("rlesur/klippy")
```

```{r, include = FALSE}
library(knitr)
library(styler)
library(tidyverse)
library(installr)
library(foreign)
library(network)
library(RSiena)
library(RsienaTwoStep)
library(igraph)
library(devtools)
```

```{r, globalsettings, echo=FALSE, warning=FALSE, results='hide'}
knitr::opts_chunk$set(echo = TRUE)
opts_chunk$set(tidy.opts=list(width.cutoff=100),tidy=TRUE, warning = FALSE, message = FALSE,comment = "#>", cache=TRUE, class.source=c("test"), class.output=c("test2"))
options(width = 100)
rgl::setupKnitr()
```

```{r klippy, echo=FALSE, include=TRUE}
klippy::klippy(position = c('top', 'right'))
#klippy::klippy(color = 'darkred')
#klippy::klippy(tooltip_message = 'Click to copy', tooltip_success = 'Done')
```

------------------------------------------------------------------------

```{=html}
<!--- This is the best way to write a comment. 
It also demonstrate, you can add html directly in your rmarkdown file. See: https://www.w3schools.com/html/html_comments.asp --->
```

```{=html}
<!--- to use a multi column page we use Pandoc's fenced Div. 
see here: https://bookdown.org/yihui/rmarkdown-cookbook/multi-column.html 
But please note, we are not here to make a beautiful website, our intention is just to have a quick lab journal up and running --->
```

::::: columns
::: {.column width="50%"}
# My Lab Journal

Last compiled on `r format(Sys.time(), '%B, %Y')`

This website was made by Lauren Dirix, as part of the course Social Networks (SOW-SCS211), and showcases both my progress (under tab "Lab journal") and my final assignment (under tab "Final assignment")

<br>

------------------------------------------------------------------------

# Open Science

This website and web journal was created by forking the repository from Jochem Tolsma and Bas Hofstra [@SNASS].
:::

::: {.column width="50%"}
![](images/cover.jpg)
:::
:::::


------------------------------------------------------------------------

# References
